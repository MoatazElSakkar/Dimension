Prject Dimension Module #4
==========================

Dependencies:
-Dimension.data.dll => required to operate correspondence

-Renderer hosts the Texture class and the texture mapping data that maps each texture to an enclosed bound
array through their ids as an array

-Also hosts the light source class that is capable of adding the light effect layer

-Finally the renderer hosts functions that are to produce the final bitmaps