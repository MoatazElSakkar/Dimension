Prject Dimension Module #3
==========================

Dependencies:
-Dimension.data.dll => required to access point class.

Linear Algebra is a module that handles all operations related to matrices.

-It stores a matrix as a float array and can produce a matrix from a Point, a 1D Array or a 2D array.

-Linear algebra as a static class of itself and can preform matrix multiplication
*may include also matrix transposition if required in texture mapping

